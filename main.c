#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>
#include "projet_c.h"

int main()
{
	/*Liste_voitures l;
	l.tete = NULL;

	/*Cycle feu = {30,6,30};
	temps_arrivee(200,&l,lambda);
	
	temps_passage(&l,feu, alpha);
	duree_attente(&l);
	correction_num(&l);
	//afficher_liste_voitures(&l);
	ecriture_fichier(&l);


	lecture_fichier(&l);
	//afficher_liste_voitures(&l);
	tableau_de_bord(&l);*/


	appli();
		
	return(0);
}
